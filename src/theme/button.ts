import { ViewStyle } from "react-native";
import { evaPallete } from "./pallete";

export const BUTTON: ViewStyle = {
  backgroundColor: evaPallete["color-button"],
  borderColor: "transparent",
  alignItems: "center",
  justifyContent: "center",
  minWidth: "100%",
  borderRadius: 0
}